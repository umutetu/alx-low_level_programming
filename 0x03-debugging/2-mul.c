#include "main.h"

/**
 *  * mul - function to multiplying two numbers.
 *   * @num1: first number.
 *    * @num2: second number.
 *     * Return: multiplication.
 *      */
int mul(int num1, int num2)
{
		return (num1 * num2);
}
